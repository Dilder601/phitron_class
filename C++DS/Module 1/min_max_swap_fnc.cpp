#include<iostream>
#include<algorithm>
#include<utility>
using namespace std;

int main() {

    //swap

    int a, b;
    cin >> a >> b;
    swap(a, b);

    cout << a << " " << b << endl;



    //min max

    /*int a, b, c, d;
    cin >> a >> b >> c >> d;
    int mn = min({ a,b,c,d });
    int mx = max({ a,b,c,d });

    cout << "Min Value is " << mn << " And Max value is " << mx << endl;
    */
    return 0;
}